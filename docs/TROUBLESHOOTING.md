# Troubleshooting Guide

- **Port 8000 already in use**: Kill existing processes using `npx kill-port 8000` or find process PID via task manager.
- **Vite builds failing**: Ensure you have run `npm install` inside the frontend directory before building.
