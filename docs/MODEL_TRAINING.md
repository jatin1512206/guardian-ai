# Model Training Guide

1. **Driver CNN**: Run `python backend/training/train_driver_model.py` to train driver monitoring classifiers.
2. **Vehicle Dynamics GRU**: Run `python backend/training/train_vehicle_model.py` to train dynamic behavior sequences.
3. **Predictor Transformer**: Run `python backend/training/train_predictor.py` to train accident forecasting layers.
